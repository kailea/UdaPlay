# Copy this file to `config.env` (same folder as the notebooks) and fill in your keys.
# `load_config()` in lib/udaplay.py reads config.env first, then .env.

OPENAI_API_KEY="voc-**********"
TAVILY_API_KEY="tvly-*********"
OPENAI_BASE_URL="https://openai.vocareum.com/v1"
